<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsna19.test2">

    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test2"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".total_login"
            android:label="@string/title_activity_total_login"
            android:theme="@style/Theme.Test2.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".customer_login" />
        <activity android:name=".kakao_login" />
        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Test2.NoActionBar">
        </activity>

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="dd5c52260709db368f679ce9abedb30c" />
        <activity android:name=".baedal_main">

        </activity>
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>